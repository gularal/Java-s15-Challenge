package Model;

public class Faculty extends MemberRecord {
    public Faculty(int memberID, String type, String dateOfMembership, int maxBookLimit, String name, String address, String phoneNumber) {
        super(memberID, type, dateOfMembership, maxBookLimit, name, address, phoneNumber);
    }
}

